import { Loader2 } from "lucide-react";

export default function Loading() {
  return <Loader2 className="mx-auto my-3 text-center font-bold text-purple-600 justify-center animate-spin"
        strokeWidth={3}  />;
}